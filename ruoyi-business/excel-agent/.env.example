# excel-agent 独立运行时的兜底配置；正式环境由 RuoYi「AI配置」下发。
# 变量名保留 ARK_* 以兼容旧脚本，值已改为 DeepSeek OpenAI 兼容接口。
ARK_API_KEY=replace-with-your-deepseek-api-key
ARK_API_URL=https://api.deepseek.com/v1/chat/completions
ARK_MODEL=deepseek-chat
ARK_TIMEOUT_SECONDS=90
